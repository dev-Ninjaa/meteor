import { api } from './index';
import type { Transaction, CreateEscrowDto, ReleaseEscrowDto, RefundEscrowDto, QueryTransactionsDto, PaginatedResponse } from '../../types/payment';

export const walletApi = {
  // Wallet-specific endpoints if needed
  // For now, payments are handled via paymentsApi
  // This can be extended for wallet-specific operations
};